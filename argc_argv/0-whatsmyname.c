#include "main.h"

/**
 * main - Write a program that prints its name
 *
 * @a: is an int
 * @argv: is the name of the file
 *
 * Return: 0
 */

int main(int i, char *argv[])
{
a = 0;
printf("%s\n", argv[0])
return (i);
}
