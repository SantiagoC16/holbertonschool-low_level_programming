#include "main.h"
#include <stdio.h>

/**
 * _abs - Write a function that computes the 
 * absolute value of an integer.
 * @c: an int.
 * Return: Always 0.
 */
int _abs(int)
{
  
int = x 
if (n < 0)
printf(%d*-1);
return (0);
}
