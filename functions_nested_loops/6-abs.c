#include "main.h"
#include <stdio.h>

/**
 * _abs - Write a function that computes the 
 * absolute value of an integer.
 * @n: an int.
 * Return: Always 0.
 */
int _abs(int n)
{
int n; 
if (n < 0)
return("%d", n*-1)
}
