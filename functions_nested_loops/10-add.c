#include "main.h"
#include <stdio.h>

/**
 * times_table - Write a function that prints the 9 times table.
 * @n, @i, @a, @b, @c: an int.
 * Return:the last damage
 */
int add(int, int)
{
int a, b, c;
c = a * b;
_putchar(c, '0')
}
