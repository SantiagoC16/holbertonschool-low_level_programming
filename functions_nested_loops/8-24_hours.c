#include "main.h"
#include <stdio.h>

/**
 * jack_bauer - Write a function that prints every minute from
 * 00:00 to 23:59
 * @n, @a, @b, @c, @d: an int.
 * Return:the last damage
 */
void jack_bauer(void)
{
  int a, b;
for (a = 0; a < 24; n++)
{
for (b = 0; b < 60; n++)
{
_putchar(a /10 + '0');
_putchar(a % 10 + '0');
_putchar(':');
_putchar(b / 10 + '0');
_putchar(b % 10 + '0');
_putchar('\n');
}
}
}

