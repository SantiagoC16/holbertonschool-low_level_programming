#include "main.h"
#include <stdio.h>

/**
 * _islower - Write a function that checks for lowercase character.
 *
 * Return: Always 0.
 */
int _islower(int c)
{
if (c >= a && <= z)
printf(1);
else (c >= A && <= Z)
printf(0);
return (0);
}
