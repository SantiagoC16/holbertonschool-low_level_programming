#include <stdio.h>
/*
 * main - prints all single digit numbers of base 10 starting from 0
 *
 * Return: Always 0.
 */
int main(void)
{
int i;

for(int i = 0; i < 10 ; i++)
putchar(i, n);
putchar("%c\n");
return (0);
}
