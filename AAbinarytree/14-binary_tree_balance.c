#include "binary_trees.h"

/***/

int binary_tree_balance(const binary_tree_t *tree);
{
    int left = 0, right = 0;
    
    binary_tree_balance(tree->right != NULL)
    binary_tree_balance(tree->left != NULL)
    height <=  1 o >= 1 
    
    return ;
}