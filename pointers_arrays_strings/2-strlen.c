#include "main.h"
#include <stdio.h>

/**
 * _strlen - Write a function that returns the length of a string.
 * @s : is a char
 * return : always 0.
 */

int _strlen(char *s)
{
char *s[]=null;
printf("%zu \n", *s);
return (0);
}

